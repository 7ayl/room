# Where to put models

This folder is reserved for 3D models (glTF/glb). To replace placeholders in the scene, add files to public/models/ and update the paths in src/components/ThreeScene.vue.

Examples:
- public/models/avatar.glb
- public/models/cat.glb
- public/models/notebook.glb
